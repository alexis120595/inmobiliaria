import React from 'react';

const Header = () => (
  <header className="header">
    <span className="header-user">Administrador</span>
    <button className="btn btn-danger">
      Cerrar sesión
    </button>
  </header>
);

export default Header;
